services:
  mysql:
    image: mysql:latest
    container_name: helpdesk-mysql
    environment:
      MYSQL_DATABASE: helpdesk
      MYSQL_USER: student
      MYSQL_PASSWORD: student
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"